# План: убрать структурный долг + дисциплина документации

Этот файл самодостаточен: новый чат (Claude или Codex) читает его и
работает по нему, ничего не помня из прошлых сессий. Задачи 31-40 —
продолжение нумерации из `docs/codex-onboarding.md` (там были 1-30,
разрез `app.py` на `templates/` + `blueprints/`, все выполнены).

## Что уже сделано раньше (не переделывать)

- HTML/JS вынесены в `templates/*.html` (читаются через `_template()`,
  БЕЗ Jinja2 — обычные файлы + `.replace()`).
- Роуты разложены по `blueprints/*.py` (17 файлов, фабрики
  `create_X_blueprint(...)`).
- Версии зависимостей зафиксированы в `requirements.txt`.
- Есть автотесты: `tests/`, 15 файлов, ~2400 строк, гоняются в CI дважды
  (до слияния в main и перед сборкой на VPS).
- Деплой переехал на раннер прямо на VPS в Амстердаме.

## Замеры на 2026-09-18 (почему этот план вообще существует)

| Что | Значение |
|---|---|
| `app.py` | 4837 строк, 184 функции верхнего уровня (2540 строк), 204 глобальных присваивания |
| `create_drop_blueprint(...)` | **61 зависимость** аргументами. `remote` — 45, `ai` и `backup_sebastian` — по 33, `home` — 31 |
| `CLAUDE.md` | **72 КБ** (~13 тыс. токенов на КАЖДУЮ сессию). 2026-09-04 был урезан до 20 КБ — вырос в 3.5 раза за две недели |
| из них раздел «Что сейчас работает» | 36 тыс. символов = 78% файла |
| роутов | 175 (две недели назад — 127) |

**Диагноз.** Blueprints получили роуты, но не самостоятельность: логика и
состояние остались в `app.py` и раздаются наружу пачками аргументов.
Правка в дропе = читать `blueprints/drop.py` (793 строки) + блок
регистрации на 61 строку + сами функции в `app.py`. «Одна фича — один
файл» выполнено формально, а не фактически. Плюс главный документ снова
раздулся и грузится целиком независимо от задачи.

## Жёсткое ограничение, которое нельзя нарушить

`app.py` — **не фабрика**: он выполняет всю работу прямо при импорте
(читает пароль из окружения, считает пути к данным от `__file__`,
заполняет глобальные словари). На этом держится вся обвязка тестов
(`tests/conftest.py` импортирует КОПИЮ проекта во временной папке).

**Не превращать `app.py` в фабрику (`create_app()`) в рамках этого
плана.** Модули `core/*` — тоже обычные модули, выполняющиеся при
импорте: семантика не меняется, `conftest.py` продолжает работать без
правок. Если `conftest.py` пришлось править — значит что-то пошло не
так, остановиться и спросить.

---

# Блок А — дешёвое и с самым большим эффектом (задачи 31-33)

```
31. [выполнено 2026-09-18] Разгрузить CLAUDE.md: раздел «Что сейчас работает» (36 тыс. символов,
    78% файла) разнести по файлам рядом с кодом — blueprints/<name>.md
    (music.md, drop.md, ai.md, remote.md, phone.md, files.md, diy.md,
    notebook.md, debts.md и т.д., по одному на blueprint). В каждом:
    назначение, точки входа (URL), где данные, инварианты («что нельзя
    сломать»), какой тест гонять перед правкой. 30-60 строк, не больше.

    В CLAUDE.md вместо раздела остаётся 5-10 строк: «описание каждой
    фичи — в blueprints/<name>.md рядом с кодом, читать только ту, что
    правишь». Добавить туда же раздел «Дисциплина документации» (текст
    ниже в этом файле, целиком).

    Цель по размеру: CLAUDE.md ≤ 25 КБ. Проверка: wc -c CLAUDE.md.
    Один коммит.

32. [выполнено 2026-09-18] Тест-сторож на размер CLAUDE.md: tests/test_docs.py, падает если
    CLAUDE.md больше 30 КБ. Сообщение об ошибке должно объяснять, что
    делать («описание фичи — в blueprints/<name>.md, хроника — в
    docs/history.md, см. раздел „Дисциплина документации“»), а не просто
    «слишком большой файл».

    Причина: правило, которое держится на дисциплине, уже один раз
    не удержалось (урезали до 20 КБ — вырос до 72 КБ за две недели).
    Правило, которое держит тест, не зависит от того, кто сидит в чате.
    Один коммит.

33. [выполнено 2026-09-18] Опись роутов — генерировать, а не вести руками.
    - scripts/gen_routes.py: поднимает приложение, проходит
      app.url_map.iter_rules() (кроме static), пишет docs/routes-inventory.md
      — те же разделы/таблицы, что сейчас, но целиком из кода.
    - tests/test_docs.py: тест, который перегенерирует опись в память и
      падает, если она разошлась с файлом в репозитории. В сообщении —
      «запусти python3 scripts/gen_routes.py и закоммить».
    - Из CLAUDE.md убрать пункт «обновить docs/routes-inventory.md руками»,
      заменить на «запустить генератор».

    Причина: файл врал дважды, и это записано в нём же («стояло 143, а в
    коде было 153»; раньше «123 вместо 127» — забыли @sock.route).
    Один коммит.

    Отступление от буквального «те же разделы»: старые тематические
    разделы (`cabinet`, `websockets/remote access`...) были ручной
    классификацией — тем же источником расхождений, что и число в шапке.
    Группировка теперь по blueprint'у (имя из `Blueprint(...)`) — это
    тоже код, но однозначный и без места для «забыли переклассифицировать».
    Колонка Guards — decorator'ы по AST исходников, а не вручную вписанные
    фразы («живое соединение», «проверка внутри функции»): то, что нельзя
    увидеть в decorator'е, теперь в `blueprints/<name>.md` (задача 31), а
    не как текст, который никто не обязан держать в актуальном виде.
```

# Блок Б — инверсия зависимостей (задачи 34-39)

Суть: вместо `create_drop_blueprint(template=..., drop_lock=..., ...61
штука...)` blueprint сам импортирует нужное: `from core import storage,
auth`. Тогда фича действительно самодостаточна — открыл один файл и
видишь все её зависимости в шапке.

```
34. [выполнено 2026-09-18] Каркас core/ + обкатка паттерна на САМОМ МЕЛКОМ blueprint.
    Создать core/ с модулями по смыслу (не все сразу — только то, что
    реально нужно первому переезжающему): core/auth.py (login_required и
    прочие guard'ы), core/templates.py (_template), core/storage.py
    (atomic-запись json, общие пути к данным).

    Перевести на импорты ОДИН мелкий blueprint — login_log.py (27 строк,
    6 зависимостей) или pwa.py (217 строк, 10 зависимостей). Цель шага —
    не выигрыш, а проверенный образец, на который будут смотреть
    следующие задачи.

    Поведение не меняется: те же URL, ответы, заголовки. Тесты зелёные.
    Один коммит.

    Как выполнено на практике: `login_required` оказался сцеплен с
    доверенными устройствами (device_check/DEVICE_COOKIE/trusted_devices)
    и с журналом входов (log_login пишет туда при автовходе по устройству)
    — оба переехали в core/auth.py целиком, иначе `login_required` не
    получился бы самостоятельным. Старые приватные имена `app.py`
    (`_device_check`, `_log_login`, ...) заведены через `import ... as`,
    чтобы не трогать полторы сотни мест вызова; новые публичные имена без
    подчёркивания — то, что берут blueprints напрямую (`login_log.py` уже
    так и делает; `devices.py`, `debts.py`, `home.py`, `remote.py` в
    задачах 35/36/38 могут взять `log_login`/`client_ip`/`device_*`
    оттуда же, не тревожа app.py). Специфичные guard'ы
    (`debts_owner_required`, `diy_editor_required`, `music_editor_required`)
    остались в `app.py` — переезжают вместе со своими фичами.

    `tests/conftest.py` пришлось-таки поправить (задача сама предупреждала
    остановиться и спросить — спросили, хозяин выбрал этот вариант):
    `core/storage.py` считает `DATA_DIR` от своего расположения на диске,
    и без копии `core/` в temp-папку копия `app.py` находила бы
    настоящий `core/` репозитория через `sys.path` и писала бы в
    настоящую `data/` — сторож `test_storage_is_isolated_from_the_repo`
    поймал это сразу. Добавлена одна строка в список копируемых папок
    (`app.py, blueprints, templates, static` → `+ core`) и `core.*` в
    список модулей, которые чистятся из `sys.modules` перед переимпортом.
    Тот же приём и в `scripts/gen_routes.py` — у него своя временная копия
    для чтения `url_map`, тоже не знала про `core/`.

    Добавлен `tests/test_login_log.py` — раньше страница `/login-log` не
    была покрыта отдельно, только косвенно через `test_every_url_for_target_exists`.

35. [выполнено 2026-09-18] devices.py (23 зависимости) и notebook.py (14) — по образцу из 34.
    Один коммит на оба (они мелкие).

    Как выполнено на практике: `devices.py` тянул за собой суточный пароль
    консоли и общее ограничение частоты попыток (`rate_blocked/hit/clear`,
    `console_login_attempts`, `SSH_GATE_PASSWORD_PREFIX`,
    `console_password_today`) — тоже переехали в `core/auth.py` целиком
    (они и раньше были общими: `debts.py`/`home.py`/`remote.py` получают их
    через `app.py`, тот теперь просто импортирует их вместо того, чтобы
    объявлять). После этого `devices.py` берёт вообще всё из `core.auth`/
    `blueprints.pwa`/`core.templates`, фабрика — без аргументов.

    `notebook.py` пошёл дальше «просто импортов»: `notebook_data`/
    `notebook_lock`/весь код чтения-записи переехали В САМ blueprint —
    фича владеет своим состоянием, а не получает его от `app.py` (как и
    ICON_LINKS в `pwa.py`). `app.py` и `blueprints/ai.py` (кнопка «В
    блокнот») по-прежнему читают `notebook_data`/`notebook_lock`, но теперь
    импортируют их из `blueprints.notebook`, а не объявляют сами. Два
    мелких общих хелпера, которые notebook.py делил с ещё не переехавшим
    diy.py (`_diy_safe_name`, `_notebook_clean_url`), заведены в
    `core/storage.py` как `safe_filename`/`clean_url` — иначе notebook.py
    зависел бы от app.py напрямую (циклический импорт).

    Добавлены `tests/test_devices.py` и `tests/test_notebook.py` — раньше
    ни одна из двух страниц не была покрыта отдельно.

36. [выполнено 2026-09-18] home.py (31), diy.py (26), music.py (27), debts.py (28).
    Отдельный коммит на КАЖДЫЙ — это уже заметные файлы.

    home.py на практике: рекорды аркады (ARCADE_*, arcade_scores/
    arcade_lock/весь код) переехали В САМ blueprint — использовались
    только там же, ни с чем не общие (в отличие от notebook_data).
    SERVERS_PASSWORD тоже больше не приходит из app.py — одна строка
    чтения переменной окружения, не общий секрет. Ограничение частоты
    попыток/суточный пароль консоли/login_required — из core.auth напрямую
    (задачи 34-35). Остались фабричными аргументами только `game_icons`
    (350+ строк пиксель-арта, общая с ещё не переехавшим Себастьяном —
    его аватарка) и `netbird_devices` (общий список машин) — 31
    зависимость → 2. Добавлен tests/test_home.py.

    diy.py на практике: самый большой перенос из блока Б до сих пор
    (~1000 строк с учётом заготовок демо-статей `DIY_SEEDS`). Вся страна
    DIY — данные, лимиты, guard `diy_editor_required`, парсер шапки
    статьи, рендер `{{вложение}}` — переехала в blueprints/diy.py целиком;
    `diy_items`/`diy_lock`/`diy_load` app.py по-прежнему читает обратно
    для резервных копий (тот же приём, что notebook_data в задаче 35).
    `safe_filename`/`clean_url` из core/storage.py (задача 35) используются
    напрямую вместо приватных алиасов app.py. 26 зависимостей фабрики → 0.
    Добавлен tests/test_diy.py — раньше не было ни одного pytest'а на DIY
    вовсе (только `scratchpad/diy.js` для раскладки).

    music.py на практике: самая тесная связь с чужим кодом из всего блока
    Б до сих пор — ещё не переехавший drop.py читает и пишет музыкальные
    внутренности НАПРЯМУЮ (папка MUSIK показывает саму фонотеку:
    `_drop_music_take/_view/_send/_delete` в app.py трогают `music_items`,
    `music_folders`, `music_lock`, `MUSIC_DIR`, `MUSIC_EXTS`, `MUSIC_MIMES`,
    `music_scan`, `music_safe_name`, `music_split`, `music_write_index`,
    `music_unlink` по имени, а не через фабрику). Все они переехали в
    blueprints/music.py и импортированы в app.py обратно под старыми
    именами через `as` — то же решение, что и для core/auth.py в задаче 34,
    только межблюпринтовое, не core/app. Инвариант порядка локов (сначала
    `music_lock`, потом `drop_lock`, никогда наоборот — взаимоблокировка)
    сохранён комментарием на обеих сторонах. `drop_used_safe`/`drop_quota`/
    `drop_lock`/`drop_musik_tracks` остались аргументами — это уже
    DROP-овское, переедет в задаче 39. 27 зависимостей фабрики → 4.
    Проверено вручную сверх автотестов: `_drop_music_take/_view/_send/
    _delete` вызваны напрямую (без HTTP) на песочнице — загрузка в MUSIK,
    просмотр, отдача, удаление отработали. Расширен tests/test_music.py
    (была только проверка гейта и ETag `/vg-player.js` — добавлены
    загрузка/переименование/удаление трека, папки с избранной звездой,
    `/api/player/tracks` по умолчанию берёт избранную папку).

    debts.py на практике: самая чистая миграция из всех четырёх — 27 из
    28 зависимостей ушли в core.auth (гейты/лимиты/суточный пароль,
    задачи 34-35) или переехали в сам blueprint (debts_data/debts_lock,
    оба guard'а, вся арифметика долгов); `debts_data` не мутируется на
    месте, а пересоздаётся целиком в `debts_load()` (`global debts_data`)
    — так было и раньше, просто теперь в одном файле с тем, что её читает,
    поэтому опасности устаревшей ссылки нет. Кстати нашлось: `password_matches`
    (проверка «пароль не совпадает с паролем кабинета») была нужна и
    app.py (`/api/login`), и debts.py — переехала в core/auth.py вместе с
    PASSWORD_SALT/HASH/ITERATIONS. `debt_find_user_by_password` осталась
    единственной зависимостью в обратную сторону: `/api/login` в app.py
    перебирает пароли должников, если пароль кабинета не подошёл, —
    импортирована оттуда. `notification_add` осталась аргументом фабрики
    — общий журнал уведомлений (`/notifications`) ещё не переехал ни в
    один blueprint. 28 зависимостей → 1.

    Задача 36 закрыта: 4 коммита, 4 файла, тесты на каждый — где их не
    было (home/diy), где были только гейт+ETag (music), расширены; там,
    где уже было полное покрытие (debts), только починена привязка
    внутреннего состояния теста (`sys.modules["blueprints.debts"]` вместо
    `app_module`).

37. ai.py (33) и backup_sebastian.py (33). Отдельный коммит на каждый.
    Осторожно: в ai.py живёт SSE-стриминг (_ai_run_stream, 167 строк в
    app.py — самая большая функция там) и обработка обрыва соединения
    через GeneratorExit. Проверить тестом, что «Стоп» по-прежнему
    досохраняет частичный ответ.

    ai.py на практике: блок был полностью самодостаточен в app.py — ничего
    из него не читал никто другой, поэтому фабрика сократилась с 33
    зависимостей до 3 (claude_ready/claude_host_name/claude_dir — часть
    ещё не переехавшего /ws/claude из remote.py, задача 38). Добавлен
    tests/test_ai.py::test_stop_saves_partial_reply: дёргает
    ai_run_stream() напрямую, забирает первый кусок ответа и закрывает
    генератор (то же GeneratorExit, что шлёт кнопка «Стоп») — finally
    всё равно сохранил частичный ответ. 33 зависимости → 3.

    backup_sebastian.py на практике: два не связанных друг с другом
    раздела в одном файле (были соседями и в app.py) — BACKUP_TOKEN/
    backup_measure/backup_build и вся конфигурация Себастьяна переехали
    в сам файл; diy_lock/diy_items/diy_load, notebook_lock/notebook_data/
    notebook_load, music_lock/music_items/music_folders/music_load
    теперь читаются напрямую из своих blueprint'ов (задачи 35-36), а не
    через фабрику app.py. drop_lock/drop_items/drop_load_index/drop_dir
    остались аргументами фабрики — сам DROP_DIR ещё не переехал из app.py
    (задача 39); game_icons — по той же причине, что и в home.py (задача
    36, общий словарь пиксель-арта). SEBASTIAN_HOST остался нужен app.py
    для /api/metrics — импортирован обратно, как notebook_data в задаче 35.
    Добавлен tests/test_backup_sebastian.py (раньше не было ни одного
    теста ни на /backup, ни на /sebastian): гейты, экспорт даёт настоящий
    zip, импорт восстанавливает блокнот и отклоняет архив без backup.json,
    /sebastian отвечает «не готов» без настроенного Ollama. Заодно
    tests/test_notebook.py перешёл на sys.modules["blueprints.notebook"]
    (app.py больше не импортирует notebook_lock — читать его напрямую
    из app_module было нечем). 33 зависимости → 5.

    Замечено попутно: backup_skip() бракует любой путь с сегментом `tmp`
    (изначально — чтобы не класть в архив служебные `*_tmp` папки), а
    песочница pytest сама лежит под /tmp — поэтому реальный
    /api/backup/export в тестах не кладёт в архив ничего, кроме
    backup.json. Поведение перенесено дословно (не регрессия миграции),
    но тест на roundtrip импорта собирает архив вручную, а не через
    настоящий export — иначе он на самом деле ничего бы не проверял.

    Задача 37 закрыта: 2 коммита, 2 файла. routes-inventory.md — без
    изменений (нулевой диф после regen) в обоих коммитах.

38. remote.py (45 зависимостей) — кабинет, NetBird, SSH/RDP/VNC-вебсокеты.
    Самое рискованное место: вебсокеты, paramiko, guacd. Отдельный
    коммит, перед пушем — открыть консоль руками и убедиться, что
    подключается (тестами это не покрыть).

39. drop.py (61 зависимость) и files.py (16) — самые жирные. Отдельный
    коммит на каждый. После drop обязательно прогнать tests/test_drop.py
    (там проверка целостности файла побайтово) и вручную: загрузить,
    скачать, удалить, публичная ссылка.
```

# Блок В — финал и новые фичи (задача 40)

```
40. Итог: app.py должен остаться тонким — создание Flask-приложения,
    конфиг, регистрация blueprints, запуск фоновых задач. Удалить то,
    что переехало в core/ и осталось мёртвым кодом. Сверить число роутов
    генератором из задачи 33 (не должно измениться ни на один).

    Зафиксировать в CLAUDE.md новую схему одним абзацем: core/ — общее,
    blueprints/ — фичи, templates/ — разметка, tests/ — проверки.
    Один коммит.
```

**Новые крупные фичи (аккаунты, профили, чаты) — не в этом плане.**
Их делать сразу отдельным модулем и **на SQLite, а не на JSON** (текущие
JSON + локи не рассчитаны на пользователей и историю сообщений). Формат
модуля — как в блоке Б после его завершения.

**Старые JSON-фичи (дроп, музыка, DIY) на БД не переводить**, пока они
работают. Переносить только то, что и так приходится сильно менять.

---

# Дисциплина документации (правила для ВСЕХ будущих чатов)

Этот раздел задача 31 копирует в `CLAUDE.md` целиком. Он существует
потому, что за две недели `CLAUDE.md` вырос с 20 КБ до 72 КБ, а опись
роутов дважды разошлась с кодом.

**Куда что писать:**

| Что | Куда | Предел |
|---|---|---|
| Правила, ловушки, ограничения хозяина, навигация «куда идти» | `CLAUDE.md` | ≤ 25 КБ, сторожит тест |
| Как устроена конкретная фича | `blueprints/<name>.md` рядом с кодом | 30-60 строк |
| «Пробовали X, откатили, вот почему» | `docs/history.md` | только если урок повторяемый |
| План/ТЗ | `docs/<name>-plan.md`, после выполнения — пометка «выполнено» | — |
| Список роутов, размеры, счётчики | **никуда** — генерируется скриптом | — |

**Три правила, которые важнее остальных:**

1. **Не писать в документ то, что можно узнать командой.** Число роутов,
   список файлов, размеры — это `grep`/скрипт, а не строчка в `.md`.
   Записанное число устаревает молча и потом врёт увереннее, чем
   отсутствие числа.

2. **Факт, который важен, — сторожить тестом, а не абзацем.** Если
   что-то «нельзя ломать» — на это пишется тест. Пример, который уже
   спас прод: `test_every_url_for_target_exists` ловит `BuildError` от
   переименования эндпоинта при переносе в blueprint (однажды так
   сломался вход в кабинет). Один такой тест полезнее десяти страниц
   описания.

3. **Перед добавлением абзаца в любой `.md` спросить себя:** переживёт
   ли это следующий рефакторинг? если разойдётся с кодом — кто заметит?
   Если ответ «никто» — не писать.

**Чего не делать:**

- не переписывать `CLAUDE.md` целиком «заодно» — дописывать в нужный
  раздел, не заводя новый под каждую фичу;
- не копировать в `CLAUDE.md` описание своей свежей фичи «чтобы не
  забылось» — для этого есть `blueprints/<name>.md`;
- не вести хронику правок в `CLAUDE.md` — она в `docs/history.md`, и
  только если из неё следует урок на будущее;
- не дублировать один факт в двух файлах: копии расходятся, и потом
  невозможно понять, какая верна.

**Когда фича добавляется (напоминание, подробности в CLAUDE.md):**
новый файл в `blueprints/`, разметка в `templates/`, тест в `tests/`,
описание в `blueprints/<name>.md`, опись роутов — перегенерировать
скриптом, в `app.py` — только регистрация.

---

## Как работать по этому плану

1. Задачи по порядку, **один коммит на задачу** (где не сказано иное).
2. После каждой: `pytest -q` локально, потом пуш и **зелёный CI**
   (он гоняет тесты дважды и реально стучится в живой сайт на VPS).
3. Поведение не меняется байт в байт: те же URL, ответы, заголовки,
   ETag, редиректы, сессии.
4. Если `tests/conftest.py` пришлось править — остановиться и спросить:
   скорее всего, сломана семантика импорта `app.py` (см. «Жёсткое
   ограничение» выше).
5. Не смешивать задачи и не делать попутных правок «раз уж открыл».
